import {StyleSheet} from 'react-native';

const PlayoffScreenStyle = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'column',
    backgroundColor: '#000000',
    paddingBottom: '5%',
  },
});

export default PlayoffScreenStyle;
